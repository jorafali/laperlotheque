import { YAxisDirective } from './y-axis.directive';

describe('YAxisDirective', () => {
  it('should create an instance', () => {
    const directive = new YAxisDirective();
    expect(directive).toBeTruthy();
  });
});
