import { XAxisDirective } from './x-axis.directive';

describe('XAxisDirective', () => {
  it('should create an instance', () => {
    const directive = new XAxisDirective();
    expect(directive).toBeTruthy();
  });
});
