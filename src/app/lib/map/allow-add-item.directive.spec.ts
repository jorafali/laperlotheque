import { AllowAddItemDirective } from './allow-add-item.directive';

describe('AllowAddItemDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowAddItemDirective();
    expect(directive).toBeTruthy();
  });
});
