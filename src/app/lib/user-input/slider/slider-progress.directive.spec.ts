import { SliderProgressDirective } from './slider-progress.directive';

describe('SliderProgressDirective', () => {
  it('should create an instance', () => {
    const directive = new SliderProgressDirective();
    expect(directive).toBeTruthy();
  });
});
