import { LogoutButtonDirective } from './logout-button.directive';

describe('LogoutButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new LogoutButtonDirective();
    expect(directive).toBeTruthy();
  });
});
