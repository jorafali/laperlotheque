import { SliderCursorDirective } from './slider-cursor.directive';

describe('SliderCursorDirective', () => {
  it('should create an instance', () => {
    const directive = new SliderCursorDirective();
    expect(directive).toBeTruthy();
  });
});
