import { SliderBarDirective } from './slider-bar.directive';

describe('SliderBarDirective', () => {
  it('should create an instance', () => {
    const directive = new SliderBarDirective();
    expect(directive).toBeTruthy();
  });
});
